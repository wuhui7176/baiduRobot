extends layout

block content
  h1= title
  p Welcome to #{title}
  -var currentUrl = '/about'
  a(class={active: currentUrl === '/'} href='/') Home a(class={active: currentUrl === '/about'} href='/about')


